import React, { Component } from 'react';
import { legacyRefMode } from '@splunk/react-ui/Dropdown';
import { ClassComponentProps } from '../utils/types';
/** @public */
declare type ButtonClickHandler = (event: React.MouseEvent<HTMLAnchorElement | HTMLButtonElement>, data: {
    action?: string;
    icon?: React.ReactNode;
    label?: React.ReactNode;
    value?: any;
}) => void;
interface ButtonPropsBase {
    /** Returns a value on click. Use when composing or testing. */
    action?: string;
    /** Changes the style of the button.
     * @themeNotes Enterprise themes support the following appearances:`default`, `primary`, `secondary`, and `pill`. Prisma themes support the `default`, `primary`, `secondary`, `destructive`, `toggle` and `flat` appearances.
     */
    appearance?: 'default' | 'secondary' | 'primary' | 'destructive' | 'pill' | 'toggle' | 'flat';
    /** Removes the right border and border-radius of the button so you can
     * append other elements to it. */
    append?: boolean;
    children?: React.ReactNode;
    /** @private An additional className to add to the button. */
    classNamePrivate?: string;
    /** Prevents user from clicking the button. */
    disabled?: boolean;
    /**
     * A React ref which is set to the DOM element when the component mounts and null when it unmounts.
     */
    elementRef?: React.Ref<HTMLAnchorElement | HTMLButtonElement>;
    /** Turns the button red. If you use this prop, apply other error
     * indicators, such as an error message, to meet accessibility
     * requirements. */
    error?: boolean;
    /** Applies the text that displays on the button. */
    label?: React.ReactNode;
    /** Applies an icon to the button. See @splunk/react-icons documentation for
     * more information. */
    icon?: React.ReactNode;
    /**
     * Restricts the horizontal size of the button. Set `inline` to `false` to
     * remove the right margin and stretch the button to the full width of
     * its container.  */
    inline?: boolean;
    /** Displays the chevron-down icon to indicate that the button behaves
     * as a menu. */
    isMenu?: boolean;
    /** Prevents callback when the button is disabled. */
    onClick?: ButtonClickHandler;
    /** Opens the 'to' link in a new browser tab.  */
    openInNewContext?: boolean;
    /** Removes the left border and border-radius of the button so you can
     * prepend elements to it. */
    prepend?: boolean;
    /** Adds the style to make the button appear selected. */
    selected?: boolean;
    /** Identifies the URL for a link. If set, Splunk UI applies an <a> tag
     * instead of a <button> tag. */
    to?: string;
    /** Returns a value on click. Use when composing or testing. */
    value?: any;
}
declare const defaultProps: Required<Pick<ButtonPropsBase, 'appearance' | 'append' | 'disabled' | 'error' | 'inline' | 'isMenu' | 'openInNewContext' | 'prepend' | 'selected'>>;
declare type ButtonButtonProps = ClassComponentProps<ButtonPropsBase, typeof defaultProps, 'button'>;
declare type ButtonLinkProps = ClassComponentProps<ButtonPropsBase, typeof defaultProps, 'a', 'href'>;
declare type ButtonProps = ButtonButtonProps | ButtonLinkProps;
declare class Button extends Component<ButtonProps> {
    private component;
    static propTypes: React.WeakValidationMap<ClassComponentProps<ButtonPropsBase, Required<Pick<ButtonPropsBase, "disabled" | "selected" | "appearance" | "inline" | "error" | "openInNewContext" | "append" | "prepend" | "isMenu">>, "button", never>> | React.WeakValidationMap<ClassComponentProps<ButtonPropsBase, Required<Pick<ButtonPropsBase, "disabled" | "selected" | "appearance" | "inline" | "error" | "openInNewContext" | "append" | "prepend" | "isMenu">>, "a", "href">>;
    static defaultProps: Required<Pick<ButtonPropsBase, "disabled" | "selected" | "appearance" | "inline" | "error" | "openInNewContext" | "append" | "prepend" | "isMenu">>;
    static [legacyRefMode]: boolean;
    private handleClick;
    private handleMount;
    /**
     * Places focus on the button.
     */
    focus(): void;
    render(): JSX.Element;
}
export default Button;
export { ButtonClickHandler };
