import React from 'react';
export interface ControlGroupContextValue {
    labelAttrs?: {
        text: string;
        id: string;
    };
}
/**
 * A React context used to inform subcomponets of ControlGroup of parent component values.
 * The context interface is `ControlGroupContextValue`.
 * Defaults to `'{}'`.
 * @public
 */
declare const ControlGroupContext: React.Context<ControlGroupContextValue>;
export default ControlGroupContext;
