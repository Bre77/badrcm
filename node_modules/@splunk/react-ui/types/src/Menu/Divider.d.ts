import { ComponentProps } from '../utils/types';
declare type DividerProps = ComponentProps<{}, 'div'>;
/**
 * A non-interactive menu item used to visually separate groups of items in the menu.
 */
declare function Divider({ ...otherProps }: DividerProps): JSX.Element;
declare namespace Divider {
    var propTypes: {};
    var filterFirst: boolean;
    var filterConsecutive: boolean;
    var filterLast: boolean;
}
export default Divider;
