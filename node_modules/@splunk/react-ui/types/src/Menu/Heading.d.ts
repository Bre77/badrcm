import React from 'react';
import PropTypes from 'prop-types';
import { ComponentProps } from '../utils/types';
interface HeadingPropsBase {
    children?: React.ReactNode;
    /**
     * Renders this heading as the `Menu` title. As a best practice, enable this
     * only for the first heading in a `Menu`.
     */
    title?: boolean;
    outerStyle?: React.CSSProperties;
}
declare type HeadingProps = ComponentProps<HeadingPropsBase, 'div'>;
/**
 * A non-interactive `Menu` item used to separate and label groups of `Menu` items.
 */
declare function Heading({ children, title, outerStyle, ...otherProps }: HeadingProps): JSX.Element;
declare namespace Heading {
    var propTypes: {
        children: PropTypes.Requireable<PropTypes.ReactNodeLike>;
        title: PropTypes.Requireable<boolean>;
        outerStyle: PropTypes.Requireable<object>;
    };
    var filterConsecutive: boolean;
    var filterLast: boolean;
}
export default Heading;
