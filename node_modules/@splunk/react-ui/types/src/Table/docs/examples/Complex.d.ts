import { Component } from 'react';
import { HeadCellSortHandler, RowRequestToggleHandler, TableRequestMoveColumnHandler, TableRequestResizeColumnHandler, RowClickHandler } from '@splunk/react-ui/Table';
interface Row {
    age: number;
    birthState: string;
    disabled: boolean;
    email: string;
    name: string;
    selected: boolean;
    status: string;
}
interface Header {
    align: 'left' | 'center' | 'right';
    key: 'age' | 'birthState' | 'email' | 'name' | 'status';
    label: string;
    minWidth: number;
    width: number;
}
interface ExampleState {
    data: Row[];
    headers: Header[];
    sortDir: 'asc' | 'desc';
    sortKey: 'age' | 'birthState' | 'email' | 'name' | 'status';
    activeRow?: string;
    activeRowData?: string;
}
export default class Complex extends Component<{}, ExampleState> {
    constructor(props: {});
    getExpansionRow(row: Row): JSX.Element;
    handleRequestMoveColumn: TableRequestMoveColumnHandler;
    handleSort: HeadCellSortHandler;
    handleResizeColumn: TableRequestResizeColumnHandler;
    handleToggle: RowRequestToggleHandler;
    handleToggleAll: () => void;
    handleRowClick: RowClickHandler;
    rowSelectionState(data: Row[]): "none" | "all" | "some";
    render(): JSX.Element;
}
export {};
